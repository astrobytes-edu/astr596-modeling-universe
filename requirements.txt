# JupyterBook v2.x (pin to 2.* to avoid older 1.x installs)
# Use the 2.x line to ensure Jupyter Book 2 is installed (no 1.x fallback)
jupyter-book==2.*
# MyST / notebook integration
# Use recent stable releases recommended by the projects
myst-nb>=1.3.0
myst-parser>=4.0.0

# Optional: MyST CLI (mystmd) - useful for local quickstart / preview
# Install if you want the `myst` CLI in addition to jupyter-book
mystmd>=1.0.0

# Jupytext: optional but recommended for Markdown-first or paired-notebook workflows
# Provides `md:myst` format and tools to pair/sync `.md` <-> `.ipynb` files
jupytext>=1.15.0

# Scientific computing stack
matplotlib>=3.5.0
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0
seaborn>=0.11.0
sympy>=1.9
plotly>=5.0.0

# JAX ecosystem
jax>=0.4.0
jaxlib>=0.4.0
optax>=0.1.0
flax>=0.6.0

# Astronomy specific
# Astropy is the canonical astronomy package for Python
astropy>=5.0
healpy>=1.15.0

# Jupyter ecosystem
jupyterlab>=3.4.0
notebook>=6.4.0
ipywidgets>=7.6.0

# Additional v2 features
sphinx-thebe>=0.2.0
sphinx-togglebutton>=0.3.0